local Settings = require("engine.settings")

local Map = {
    colors = {
        stroke = Settings.ui.colors.secondary,
        fill = Settings.ui.colors.primary
    }
}

function Map:load()

end

function Map:update(dt)
end

function Map:draw()

end

return Map
